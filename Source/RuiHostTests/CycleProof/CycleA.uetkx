import { CycleB } from "./CycleB"

export FRuiNode CycleA(int32 Depth = 0) {
	return (
		<VerticalBox>
			<TextBlock Text="A" />
			@if (Depth > 0) {
				return (
					<CycleB Depth={ Depth - 1 } />
				);
			}
		</VerticalBox>
	);
}
