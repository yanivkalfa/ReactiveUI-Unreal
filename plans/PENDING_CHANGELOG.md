# Pending changelog ledger

One bullet per merged user-relevant change, staged by the `plan-progress` skill at
phase/milestone completion — while the knowledge is fresh. **Drained by `release-process` §0**
into the real lanes (A: root CHANGELOG.md · B: `changelog.mjs add` · C: Discord) and then
EMPTIED. Bullets that never reach a lane are how release notes go missing — drain, don't append
around.

**Format:** `- [lane A|B|C] [artifact] one-line summary (PR #n / sha)`

---

- [lane A] [plugin] Phase 0: repository ecosystem + 8-module plugin skeleton (compiles on UE 5.6; CI gates, skills, templates, docs shell) (PR #4 / 657f279)
- [lane A] [plugin] chore: action pins v7/v6 across workflows, docs deps minor-bumped, dependabot major-ignore policy for the docs shell (chore/post-merge-tidy)
- [lane A] [plugin] Phase 1: the core library — fiber reconciler (subtree-skip, O(1) context, keyed diff, error-boundary latch, restart reclaim), 23 hooks, signals, Suspense; 23 mock-host automation tests + Bench.Core baselines (feat/core-library)
- [lane A] [plugin] Phase 2: Slate host — adapter registry, bind-once-swap-inner event proxy, SRuiRoot mount surfaces, 15 core widgets, style v1 (classes + removal-reset + never-reconstruct), GO-05 widget pool, focus fences, STATGROUP_ReactiveUI, demo gallery + PIE game mode; 38/38 suites (feat/core-library)
- [lane A] [plugin] BREAKING (pre-release): element/props/style vocabulary renamed 1:1 to Unreal names (D-33) — VerticalBox/HorizontalBox/TextBlock/EditableTextBox/RuiCanvas; WidthOverride/BorderBackgroundColor/ColorAndOpacity/DesiredSizeOverride/OnCheckStateChanged; style keys RenderOpacity/RenderTranslation/RenderScale/RenderTransformAngle/RenderTransformPivot/Font.Size/Justification/AutoWrapText/FillColorAndOpacity; + SelfHitTestInvisible visibility value (refactor/unreal-1to1-naming)
- [lane A] [plugin] Phase 3: the .uetkx compiler — C++ lexer/markup parser/jsx scan/file scan (ReactiveUIInterp), codegen to committed .uetkx.inl + stable per-module aggregators, schema-v2 diagnostics sidecars, staleness machinery (error verdicts, mtime-tie hash break, env hold, orphan sweep, UETKX2106 duplicate binding, compiler fingerprint), RUICompile [-full|-check] / RUIExportSchema / RUIContractDump [-check] commandlets, canonical formatter + uetkx.config.json walk-up, 57-case scanner + 16-case formatter shared corpora, 4-fixture contract harness; ALL 11 gallery screens converted to compiled .uetkx (feat/uetkx-compiler)
- [lane A] [plugin] Phase 4: live hot reload — D-20 expression VM (whitelist registry), markup interpreter (interpreted clicks drive interpreted state; setter-call events with the Value payload), FRuiHmr swap/link/reset + family status line, reconciler override seam + HmrRefreshAll, editor watcher (3 triggers, 30s deadman, MessageLog "ReactiveUI" dedup + resolved line), rui.Hmr.AutoLiveCoding cvar (feat/uetkx-compiler)
- [lane B] [both] Phase 5: .uetkx IDE tooling — uetkx-language-server (TS front-end corpus-locked to the C++ compiler; schema-driven completions/hover, hash-gated sidecar diagnostics, golden-corpus formatting), VS Code extension (TextMate grammar with embedded source.cpp, self-contained JS server bundle — one vsix all platforms), VS2022 MEF ILanguageClient host (feat/uetkx-compiler)
- [lane A] [plugin] Phase 6: Epic interop core — URuiHostWidget (Rui inside UMG, design-time placeholder, ReleaseSlateResources unmount), RUI::Umg::UserWidget (UMG inside Rui via SObjectWidget), URuiWorldSubsystem (PIE-end/level-travel teardown contract), RUI::Umg::UseField over FieldNotify (plugin-independent; stale-VM defaults) (feat/uetkx-compiler)
- [lane A] [plugin] Phase 7: animation/media hooks are real — UseTween/UseAnimate/UseTweenValue (host-clock, retarget-from-current, cubic easing, self-re-arming frame chain) + UseSfx sink; every hook now stub-free; exit-animation Presence protocol designed (plans/EXIT_ANIMATION_DESIGN.md) (feat/uetkx-compiler)
- [lane C] [community] The Unreal sibling's headline works end to end: JSX-like .uetkx that compiles to native C++ for shipping AND hot-reloads live in the editor (save → screen < 1s, state preserved); 11-screen gallery fully on the compiled path; VS Code/VS2022 tooling on the shared family server; 53-suite headless battery (feat/uetkx-compiler)
- [lane A] [plugin] .uetkx declarations v2: hook/module companion declarations (Ctx-threaded user hooks, verbatim C++ modules), one-component-per-file (UETKX2105) + components/ folder convention, refs-topo aggregator ordering + UETKX2107 cycle guard, suffix-cosmetic naming (UETKX0103), HMR rebuild note for support files; gallery restructured to per-component folders with real .style/.hooks companions (feat/uetkx-compiler)
- [lane B] [both] uetkx grammar recolor (family mini-tokenizer architecture, element vs component tag split, hook/module scopes) + live-vs-sidecar diagnostic dedup (feat/uetkx-compiler)
- [lane A] [plugin] .uetkx import/export + strict resolution (leg 1 of 3): `import { A, B } from "./x"` / `~/` root-alias / extensionless specifiers; `export` opt-in privacy (non-exported decls tree-shaken into a per-file detail namespace); FULL mixed-decl files (any sequence of components + hooks + modules, one-component-per-file now a lint); strict day-one resolution in the compiler (UETKX2300-2309: unknown specifier, not-exported, not-declared, duplicate/unused/misplaced import, used-not-imported with fix-it, module-boundary) — enforced by RUICompile -check; `-run=RUIMigrateImports` idempotent codemod migrated the whole 15-file gallery strict-clean; UETKX2106 keys exported names only + CompileAllRoots single ledger; sidecar v3 (export_hash + dep_hashes) with reverse-edge staleness + verdict-poisoning fix; HMR names the import blast radius (feat/uetkx-imports)
- [lane B] [both] .uetkx import intelligence: scanner mirror (imports/export/mixed-decl/Order), formatter import canonicalization (#include block, blank, import block, blank) + export prefix, `~/` config `root` key, TextMate import/export/from keywords; family-corpus mirror gate (scripts/corpus-hash.mjs + plans/family-corpus.hash, A4/TD-009) (feat/uetkx-imports)
- [lane C] [community] .uetkx grows real modules: import what you use across files, `export` to share and privacy-by-default, one file can hold a component with its hooks and styles — and a one-command codemod (`-run=RUIMigrateImports`) upgrades an existing project in place, strict-clean (feat/uetkx-imports)
- [lane A] [plugin] Router subsystem (TD-001): the family's 17 router hooks (UseLocation/UseNavigate/UseParams/UseSearchParams/UseMatch/UseBlocker/UseOutlet/…) + a matching engine (path patterns, params, relative resolve) + Router/Routes/Link/Outlet, ported engine-blind into ReactiveUICore; in-memory history, location-only re-render (`RuiRouter.h/.cpp`; suites `ReactiveUI.Router.Match`/`.Spine`) (feat/uetkx-imports / a26752b)
- [lane A] [plugin] Stylesheet third layer (TD-002): `@theme` tokens + `$token` refs + a `@uss` loader — the cascade is theme < classes < inline; RegisterTheme/SetActiveTheme/LoadStylesheet (`RuiStyle.h/.cpp`; suite `ReactiveUI.Style.Stylesheet`) (feat/uetkx-imports / 1f267e6)
- [lane A] [plugin] Exit animations (TD-003): the React-community `<Presence>` delayed-unmount boundary — UsePresence/NotifyDone, deferred deletion, re-entry cancel, timeout fence — plus a latent reconciler fix (async setState through a bailed-out intermediate now marks alternate twins) (`RuiPresence.h/.cpp`; suite `ReactiveUI.Core.Presence`) (feat/uetkx-imports / 7cb37f7)
- [lane A] [plugin] Drag-and-drop + keyboard shortcuts (TD-004): `UseShortcut` (chord → callback over a Slate pre-processor) + typed DnD over FDragDropOperation — `DragSource`/`DropTarget` elements carrying an FRuiValue payload + type-tag filter, OnDrop/enter/leave/hover (`RuiInput.*`, `RuiDragDrop.*`; suites `ReactiveUI.Slate.Shortcut`/`.DragDrop`) (feat/uetkx-imports / 44ca005, 0ca7f85)
- [lane A] [plugin] Widget surface (TD-012): the batch-2 everyday set (14 — WidgetSwitcher/ScaleBox/Throbber/WrapBox/MultiLine+SearchBox/SafeZone/DPIScaler/Separator/SpinBox/UniformWrapPanel/RichText/Grid+UniformGridPanel) + the specials (ExpandableArea two-named-slot, SegmentedControl, NumericEntryBox, ComboBox, SuggestionTextBox), each wired through every touchpoint and tested via a new headless Slate interaction harness (`ReactiveUI.Widgets.*`) (feat/uetkx-imports / bb72d65..0169016)
- [lane A] [plugin] Item-model views + focus + asset brushes (TD-022): virtualized `ListView`/`TileView` (a render-prop over SListView/STileView; each generated row is its own FRuiRoot sub-root; reactive rebuild with no churn) + `UseFocus`/imperative focus + GC-rooted texture/material asset brushes (`RuiListView.*`, `RuiInput`, `RuiAssetBrush`; suites `ReactiveUI.Widgets.ListView`/`.Slate.Focus`/`.Umg.AssetBrush`) (feat/uetkx-imports / 7de0a9d, c91c53e, 291c082)
- [lane A] [plugin] CommonUI + MVVM citizenship (TD-021): `URuiActivatableScreen` (our tree as a CommonUI screen) + UseActivation/UseInputMethod; MVVM global-collection registration (`URuiMvvmViewModel` + RegisterGlobalViewModel); reflection-based per-class UMG prop-map bridge; the plugin-independent MVVM reverse bridge (URuiSignalViewModel). CommonUI + ModelViewViewModel enabled as optional plugin refs (`ReactiveUICommonUI`/`ReactiveUIMVVMBridge`/`ReactiveUIUMG`; suites `ReactiveUI.CommonUI.*`/`.Mvvm.*`/`.Umg.PropMap`) (feat/uetkx-imports / 5539dc8, eafca27)
- [lane A] [plugin] In-editor `.uetkx` read-only live preview (TD-006): a nomad tab + panel that scans → interprets → mounts a component live, surfacing parse diagnostics + interp notes (`FUetkxPreview`, `SUetkxPreviewPanel`; suite `ReactiveUI.Editor.Preview`) (feat/uetkx-imports / 4f15f25)
- [lane A] [plugin] `{children}` forwarding (TD-015): splice a component's children into its output (feat/uetkx-imports / 80205b5)
- [lane B] [both] Embedded-C++ intelligence (TD-020): the clangd-proxy layer — a length-preserving C++ virtual document + bidirectional source map + a clangd child-process proxy (graceful degradation), wired into `server.ts` so hover/definition inside a setup/hook/module body forward to clangd and translate back (falls to the markup baseline when clangd is absent) (feat/uetkx-imports / 2d37d92, d9c41b4)
- [lane B] [vs2022] Editor polish (TD-020 tail): a Tools>Options>ReactiveUI>UETKX page + format-on-save for `.uetkx`; brace completion + smart indent via the shared language-config (vs2022 0.2.0) (feat/uetkx-imports / df1f8b1)
- [lane C] [community] The post-v1 backlog lands: a full router, `@theme`/`@uss` stylesheets, exit animations, drag-and-drop, virtualized lists, first-class CommonUI/MVVM citizenship, ~20 more widgets, and an in-editor live preview of your `.uetkx` — the Unreal sibling reaches feature parity with the family's post-v1 surface (feat/uetkx-imports)
- [lane A] [plugin] Portal unmount leak fixed: deleting a subtree containing a portal (or unmounting a root) now explicitly detaches portal children from their targets (`DetachPortalChildren` wires the previously call-site-less `RemovePortalChild` seam; caught by the feature's first tests, `ReactiveUI.Core.Portal`) (feat/interop-demos-and-cleanup / 9591960)
- [lane A] [plugin] `URuiHostWidget::Remount()` fixed — was a no-op (UMG caches TakeWidget's widget); the host now returns a stable wrapper and swaps content in place, so runtime `ComponentName` changes work (suite `ReactiveUI.Umg.Lifecycle`) (feat/interop-demos-and-cleanup / 9591960)
- [lane A] [plugin] Packaged README rewritten (was "pre-alpha scaffold — not yet functional"); unverified accessibility claim softened repo-wide (feat/interop-demos-and-cleanup / 845a59b)
- [lane C] [community] The docs site fills out: four-pillar Integration guides (UMG both directions · CommonUI · MVVM/FieldNotify · Portals), a schema-GENERATED per-widget Components catalog (29 tags, the same vocabulary the IDE extensions serve), corrected guides everywhere, and armed docs-drift count tripwires. The gallery grows to 16 screens incl. UmgHostDemo (URuiHostWidget ours-inside-theirs + the reverse-MVVM bridge live) and a REAL CommonUI activatable stack in CommonUiDemo (feat/interop-demos-and-cleanup)
