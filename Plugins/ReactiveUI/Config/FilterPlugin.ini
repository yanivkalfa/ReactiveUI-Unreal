[FilterPlugin]
; Extra files (beyond Source/Content/Resources) included when the plugin is packaged via
; RunUAT BuildPlugin. Paths are relative to the PLUGIN root. Anything user-facing that should
; travel with the packaged plugin must be listed here — packaging is an allowlist, like
; .gitattributes' export rules (denylists leak).
/README.md
/CHANGELOG.md
/LICENSE
